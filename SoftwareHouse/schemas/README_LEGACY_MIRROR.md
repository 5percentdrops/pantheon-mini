# Legacy Schema Mirror

`Pantheon Mini/contracts/` is the canonical V8.2 schema/contract source.

This folder is retained only for backward compatibility with V7-era tooling. New tools must read from `Pantheon Mini/contracts/`.

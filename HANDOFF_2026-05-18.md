---
date: 2026-05-18
project: pantheon-mini
release: V8.16
tags: [handoff, role-skills, openclaw-cleanup, naming]
---

# Session Handoff — 2026-05-18

**Anchor commit:** `098a24d` on `main` of [pantheon-mini](https://github.com/5percentdrops/pantheon-mini)
**Working dir:** `C:/Users/Admin/Documents/NBrain/pantheon-mini`
**Release shipped:** V8.16 — per-responsibility executable skills + install wiring

---

## Done tonight

### 1. ROLES.md (repo root)
Real-world responsibility → agentic execution mapping for 5 roles (Arthur, Magnus, Marcus, Jack, Cody). Iterated twice: first pass too shallow, second pass expanded so agent column matches real-world column depth, N/A rows dropped.

### 2. 67 executable skill files
Tree at `SoftwareHouse/skills/role_responsibilities/<agent>/`:

| Agent | Skills | Coverage |
|---|--:|---|
| arthur  | 13 | scope lock, master status, token budget, fan-out, escalation ladder, merge gate, user-surface authority |
| magnus  | 10 | lane synthesis, route proposal, build-vs-buy, kill authority, architecture sign-off, post-mortem |
| marcus  | 13 | SDD, contracts, tickets, red TDD, tactical fix, sanity review |
| jack    | 12 | intake, implementation loop, test discipline, scope boundary, escalation packet, lessons pre-read |
| cody    | 19 | 6 review modes, validator runner ownership, classification gate, hard-fail triggers |

Each skill = frontmatter (`skill_id`, `owner_agent`, `responsibility`, `pipeline_stage`, `inputs`, `outputs`, `gates`, `escalation`) + procedure + JSON schemas + hard rules + escalation tier. Per-agent `INDEX.md` is the router. Top-level `README.md` describes the system.

### 3. Install wiring
- `SoftwareHouse/paperclip/agents.json` — added `responsibility_skills_dir` to 5 active-mini agents.
- `scripts/seed_active_homes.py` — copies the skill tree into `~/.hermes-mini-<slug>/skills/responsibilities/` on install.
- `scripts/validate_responsibility_skills.py` (NEW) — install-time frontmatter + owner_agent validator. Currently green: **5 agents, 67 skills, 0 issues**.
- `scripts/one_click_install.sh` — validator wired in at step 5b''.

### 4. Skill Routers in seeds
Appended to all 5 seeds in `SoftwareHouse/skills/hermes_seed/`. Trigger → skill_id table + hard rule: *"if a stage has a responsibility skill, the skill IS the procedure."*

### 5. Docs
- `README.md` — V8.16 section, parity badge bumped to V8.16, SOUL layout updated to show `responsibilities/`.
- `PATCH_NOTES_MINI_V8_16.md` (NEW) — full release notes.
- `docs/ROUTING.md`, `docs/PRD_INTAKE.md`, `docs/MINI_ROLE_LIST.md` — V8.16 callouts pointing to procedure cards.

### Commit chain on main
| SHA | Message |
|---|---|
| `1acf8d0` | docs: add ROLES.md mapping real-world responsibilities to agent execution |
| `d4fe197` | docs: expand ROLES.md agentic execution depth, drop N/A rows |
| `0950607` | feat: add 67 executable role responsibility skills |
| `99ac935` | feat: wire per-responsibility skills into install + agent seed routers |
| `098a24d` | docs(v8.16): document per-responsibility executable skills |

---

## Left for next session

### 🧹 Task 1 — Remove OpenClaw references
OpenClaw is a separate project, **not** part of Pantheon Mini. **67 files** still mention it.

Hotspots:
- `manifest.json`, `README.md`, `README_INSTALL.md`
- `policies/deepseek_v4_pro_standard_developer_policy.md`
- `policies/final_model_assignment_policy.md`
- `policies/hermes_only_harness_policy.md`
- `policies/universal_escalation_pattern_policy.md`
- `rules/arthur_single_model_gpt5mini.md`, `rules/model_map.md`
- `scripts/install.sh`, `scripts/validate_org_sanity.py`
- `shared/policies/coreseed_architecture.md`
- `PATCH_NOTES_MINI_V8_10.md`, `PATCH_NOTES_MINI_V8_11.md`
- `SoftwareHouse/skills/openclaw_seed/` — **entire directory; 2 skills active** (devops, functional-tester) — referenced from `agents.json` so they need re-pointing to `hermes_seed/` equivalents OR those agents marked dormant.

**Approach:**
1. Sweep + classify each mention → harmless prose vs functional dep.
2. For the 2 active openclaw seed skills, decide: rewrite as hermes_seed equivalents, OR mark those agents dormant (consistent with the "10 active, 23 dormant" Mini model).
3. Delete `shared/policies/coreseed_architecture.md` if openclaw-only, or rewrite if it carries useful hermes content.
4. Bulk find-and-replace cosmetic prose.

### 🧹 Task 2 — Unify "Software House" → "Pantheon Mini"
**109 files** mention `software house` / `SoftwareHouse`. Two distinct surfaces:

- **`SoftwareHouse/` directory** — load-bearing. Hardcoded in scripts, `agents.json`, schemas, install paths. Renaming = wide-radius refactor (~50+ path edits).
- **Prose mentions** — purely cosmetic. Safe find/replace to "Pantheon Mini" in markdown.

**Recommendation:** ask user before starting which surface to touch.
- Prose-only sweep = ~1 hour, zero risk.
- Dir rename = riskier, breaks any existing local install path, touches every script.

### Release plan
Gate both cleanups behind a single tagged commit: `chore(v8.17): unify naming + remove openclaw`. Update `PATCH_NOTES_MINI_V8_17.md`. Bump README parity badge.

---

## Open questions for tomorrow

1. **OpenClaw active seeds** — rewrite the 2 active openclaw seeds (devops, functional-tester) as `hermes_seed/` equivalents, or mark those agents dormant?
2. **Software House dir rename** — keep `SoftwareHouse/` as-is and only fix prose, or do the full rename to `pantheon/` (or similar)?
3. **Release tag** — bundle both cleanups into V8.17, or split into V8.17 (openclaw removal) + V8.18 (naming unification)?

---

## State

- Working dir clean, no uncommitted changes.
- Checkpoint complete.
- Validator passes: 5 agents, 67 skills, 0 issues.
- Push verified on origin/main: 73 files visible in `SoftwareHouse/skills/role_responsibilities/` (67 skills + 5 INDEX + 1 README).
